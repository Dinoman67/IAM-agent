{
  "name": "iam-agent",
  "private": true,
  "description": "Cloudflare Workers + Containers host for the IAM least-privilege mitigator",
  "scripts": {
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "0.0.30"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wrangler": "4.127.0"
  }
}
